import express from "express";

// Placeholder — no endpoints defined yet. Added only so server.js (which already
// imports this file) can boot; About APIs are out of scope for this phase.
const router = express.Router();

export default router;
